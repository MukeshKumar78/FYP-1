import { LoginScreen } from 'app/features/auth/screen';

export default () => 
  <div style={{ padding: 30, display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%' }}>
        <LoginScreen/>
    </div>;
